<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:minHeight="52dp"
    android:orientation="horizontal"
    android:paddingBottom="4dp"
    android:paddingEnd="12dp"
    android:paddingTop="4dp">

    <!-- Spacer d'indentation — largeur calculée en code selon la profondeur -->
    <View
        android:id="@+id/indent_spacer"
        android:layout_width="0dp"
        android:layout_height="1dp" />

    <!-- Flèche expand/collapse ▶ / ▼ -->
    <TextView
        android:id="@+id/folder_expand_indicator"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:gravity="center"
        android:text="▶"
        android:textColor="@color/text_secondary"
        android:textSize="10sp" />

    <!-- Icône dossier -->
    <ImageView
        android:id="@+id/folder_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="10dp"
        android:layout_marginStart="8dp"
        android:src="@drawable/ic_folder"
        android:tint="@color/melodie_purple" />

    <!-- Nom du dossier -->
    <TextView
        android:id="@+id/folder_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:singleLine="true"
        android:textColor="@color/text_primary"
        android:textSize="15sp" />

    <!-- Case à cocher sélection -->
    <CheckBox
        android:id="@+id/folder_checkbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp" />

</LinearLayout>
